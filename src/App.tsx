import React from "react";
import Router from "@/router";
import "./App.less";
interface iProps {}
interface iState {}
class App extends React.Component<iProps, iState> {
  render() {
    return <Router />;
  }
}

export default App;
